<p id="notice"><%= notice %></p>

<h1>Listing Competences</h1>

<table>
  <thead>
    <tr>
      <th>Created at</th>
      <th>Updated at</th>
      <th>Name</th>
      <th>Description</th>
      <th>Thumbnail</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @competences.each do |competence| %>
      <tr>
        <td><%= competence.created_at %></td>
        <td><%= competence.updated_at %></td>
        <td><%= competence.name %></td>
        <td><%= competence.description %></td>
        <td><%= competence.thumbnail %></td>
        <td><%= link_to 'Show', competence %></td>
        <td><%= link_to 'Edit', edit_competence_path(competence) %></td>
        <td><%= link_to 'Destroy', competence, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Competence', new_competence_path %>
