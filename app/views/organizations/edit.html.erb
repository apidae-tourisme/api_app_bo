<%= render 'shared/seed_form', title: 'Modifier une organization', seed_fields: [:name, :description, :address, :email,
                                                                                 :telephone, :thumbnail, :latitude,
                                                                                 :longitude] %>
