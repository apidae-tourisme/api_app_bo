<%= render 'shared/seed_form', title: 'Créer une organization', seed_fields: [:name, :description, :address, :email,
                                                                                 :telephone, :thumbnail, :latitude,
                                                                                 :longitude] %>
