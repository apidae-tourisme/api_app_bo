<%= render 'shared/seed_form', title: 'Modifier une personne', seed_fields: [:name, :description, :thumbnail, :firstname,
                                                                             :lastname, :role, :email, :telephone] %>
